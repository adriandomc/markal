---
import "@phosphor-icons/web/regular";
import "../styles.scss";
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="utf-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no"
    />
    <meta http-equiv="Content-Security-Policy" content="default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval' https://apis.google.com https://accounts.google.com; style-src 'self' 'unsafe-inline'; img-src 'self' data: https:; connect-src 'self' https://www.googleapis.com wss: stun: turn:;" />
    <title>Markal</title>
    <meta
      name="description"
      content="Markal is a tool for building markable calendars. Just pick a date range, set legend colors, and start marking your calendar!"
    />
    <link rel="icon" href="/favicon.ico" sizes="any" />
    <link rel="icon" href="/favicon.svg" type="image/svg+xml" />
    <link rel="apple-touch-icon" href="/apple-touch-icon.png" />
    <link rel="manifest" href="/manifest.webmanifest" />

    <meta property="og:type" content="website" />
    <meta property="og:site_name" content="Markal" />
    <meta property="og:title" content="Markal" />
    <meta
      property="og:description"
      content="Markal is a tool for building markable calendars. Just pick a date range, set legend colors, and start marking your calendar!"
    />
    <meta property="og:image" content="/og-image.png" />
    <meta property="og:locale" content="es" />
    <meta property="og:locale:alternate" content="en" />

    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content="Markal" />
    <meta
      name="twitter:description"
      content="Markal is a tool for building markable calendars. Just pick a date range, set legend colors, and start marking your calendar!"
    />
    <meta name="twitter:image" content="/og-image.png" />
    <script is:inline>
      (function () {
        try {
          var stored = localStorage.getItem("markal.locale");
          var nav = (navigator.language || "es").toLowerCase().split("-")[0];
          var locale = stored === "es" || stored === "en"
            ? stored
            : (nav === "en" ? "en" : "es");
          document.documentElement.setAttribute("lang", locale);
          document.documentElement.setAttribute("data-locale", locale);
        } catch (_) {
          document.documentElement.setAttribute("data-locale", "es");
        }
      })();
    </script>
    <style is:global>
      html[data-locale="es"] [data-locale-content="en"],
      html[data-locale="en"] [data-locale-content="es"] {
        display: none;
      }
      .app-header {
        display: flex;
        flex-direction: column;
        align-items: center;
        padding: 0.75rem 1rem 0;
        text-align: center;
        gap: 0.25rem;
      }
      .app-header-brand {
        display: flex;
        align-items: center;
        gap: 0.5rem;
      }
      .app-header-title {
        font-family: 'Kefa', 'Kefa III', serif;
        font-size: 1.25rem;
        font-weight: 600;
        color: var(--ink);
      }
      .app-header-description {
        font-size: 0.8rem;
        color: var(--muted);
        margin: 0;
        max-width: 400px;
      }
      .site-footer {
        position: relative;
        padding: 1.25rem 1.5rem 1.75rem;
        text-align: center;
        font-size: 0.82rem;
        color: var(--muted);
        display: flex;
        justify-content: center;
        gap: 1.25rem;
        flex-wrap: wrap;
      }
      .site-footer a {
        color: inherit;
        text-decoration: underline;
        text-decoration-color: color-mix(in srgb, currentColor 35%, transparent);
        text-underline-offset: 3px;
      }
      .site-footer a:hover,
      .site-footer a:focus-visible {
        color: var(--ink);
        text-decoration-color: currentColor;
      }
    </style>
  </head>
  <body>
    <header class="app-header">
      <div class="app-header-brand">
        <img src="/favicon.svg" alt="Markal Logo" width="24" height="24" />
        <span class="app-header-title">Markal</span>
      </div>
      <p class="app-header-description" data-locale-content="es">
        Herramienta para crear calendarios personalizables y visuales.
      </p>
      <p class="app-header-description" data-locale-content="en">
        A tool for building custom markable calendars.
      </p>
    </header>
    <markal-app></markal-app>
    <footer class="site-footer">
      <a href="https://markal.app/privacy" rel="privacy-policy">
        Política de privacidad · Privacy policy
      </a>
      <a
        href="https://github.com/adriandomc/markal"
        target="_blank"
        rel="noopener noreferrer"
      >GitHub</a>
    </footer>
    <script>
      import "../app.ts";
    </script>
  </body>
</html>
